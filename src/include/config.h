#pragma once

#include <cstddef>
#include <string>

namespace storeservice {

extern bool ENABLED_VARIABLE_BUFFER;
extern size_t DEFAULT_BUCKET_NAME_SIZE;
extern size_t DEFAULT_REMOTE_FILE_MAX_SIZE;
extern size_t DEFAULT_REMOTE_FILE_CACHE_NAME_SZIE;
extern std::string DEFAULT_REMOTE_FILE_CACHE_PATH;

}